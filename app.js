function plus(firstNumber, secondNumber){
    console.log(firstNumber + secondNumber);
}

function divide(a,b){
    console.log( a / b);
}

// 함수 정의에서 사용한 매개변수의 이름은 그 함수 안에서만 사용하다!
// 만약 firstNumber이란 변수를 함수 plus함수 밖에서 사용하면 오류가 발생한다!

plus(3,4);
divide(98,20);
